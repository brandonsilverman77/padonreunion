# Full-resolution photo backups — kept locally, not published to the site
originals/

# macOS junk
.DS_Store

# Original full-size source videos (huge) — only the compressed videos/ folder is published
*.MOV
*.mov
compress.log
compress_videos.sh

# Whisper model (large, local only)
.whisper-models/
